# 1.1 vs 1.2 消融实验记录 (4 路)

> 用户在远程服务器 `/media/disk7t/PADP_v3/` 跑, **本机 `/home/hy/Desktop/PADP_v3/` 是开发/镜像仓库**
>
> **所有命令默认 CWD = `/media/disk7t/PADP_v3/` (PADP_v3 根目录)**, 4 路训练 CWD = `/media/disk7t/PADP_v3/VLA/` (VLA 子目录)
>
> 命令用相对路径 (跟 `E_cti/train/padp_for_test_train.py` 位置对齐), 不写绝对路径 (避免服务器 mount 路径变更)

## 消融矩阵

| # | 标签 | 线 | 数据 | Action | 训练入口 | Server | Config |
|---|---|---|---|---|---|---|---|
| A | 1.1_10D | padp_for_test (1.1) | hdf5 | 10D rot6d | `padp_for_test_train.py` | `padp_for_test_server.py` | `padp_for_test_golden.yaml` |
| B | 1.1_7D | padp_for_test (1.1) | hdf5 | 7D axis_angle | `padp_for_test_train.py` | `padp_for_test_server.py` | `padp_for_test_7d_golden.yaml` |
| C | 1.2_10D | padp_for_libero (1.2) | lerobot 10D parquet | 10D rot6d | `padp_for_libero_train.py` | `padp_for_libero_server.py` | `lerobot_libero10d.yaml` |
| D | 1.2_7D | padp_for_libero (1.2) | lerobot 7D parquet | 7D axis_angle | `padp_for_libero_train.py` | `padp_for_libero_server.py` | `lerobot_libero7d.yaml` |

**生产配置** (4 路全统一):
- `n_demo=200` (测试阶段 5)
- `num_epochs=251` (RTV8 标准)
- `loss_threshold=0.01` (Phase 2 gate)
- `rollout_interval=5` (每 5 epoch 评估)
- `rollout.first_epoch=5`
- `skip_rollout=false`, `rollout.enabled=true` (生产启用)
- Sticky `phase2_activated` 标志 (避免 loss 0.01 附近波动)

## 关键架构决策

1. **两条独立线** (用户红线):
   - `padp_for_test_*` (1.1) — 跟 VLA-v1.1 1:1 一致, 不动
   - `padp_for_libero_*` (1.2) — 新建, lerobot 路径
2. **数据集** (4 路全用统一 200 demos):
   - hdf5 源: `VLA/data/robomimic/datasets/square_d0/square_d0_abs.hdf5` (1.6GB, 400 demos)
   - 1.1 两路 (A/B) 训时直接读 hdf5
   - 1.2 两路 (C/D) 训时从 lerobot parquet 读
3. **Server port 分配** (4 路不撞):
   - A. 1.1_10D: 8765 (yaml 静态)
   - B. 1.1_7D: 8764 (yaml 静态)
   - C. 1.2_10D: 8763 (1.2 server auto-avoid)
   - D. 1.2_7D: 8762 (1.2 server auto-avoid)
4. **Action dim 配置驱动**:
   - 1.1 dataset 改 config-driven 校验, config 跟 hdf5 实际必须一致 (不匹配 raise)
   - 1.2 dataset 改 config 跟 parquet 实际必须一致 (不匹配 raise)

---

## 阶段 0: 数据生成 ✅ (服务器: /media/disk7t/PADP_v3, CWD = /media/disk7t/PADP_v3)

### 7D lerobot (200 demos)
```bash
# CWD = /media/disk7t/PADP_v3
/opt/miniconda3/envs/equidiff/bin/python -u \
  VLA/C_sim/robomimic/convert_to_lerobot.py \
  --hdf5 VLA/data/robomimic/datasets/square_d0/square_d0_abs.hdf5 \
  --output_dir VLA/data/lerobot/square_d0_lerobot7d \
  --action-dim 7 --n_demo 200
```

### 10D lerobot (200 demos)
```bash
# CWD = /media/disk7t/PADP_v3
/opt/miniconda3/envs/equidiff/bin/python -u \
  VLA/C_sim/robomimic/convert_to_lerobot.py \
  --hdf5 VLA/data/robomimic/datasets/square_d0/square_d0_abs.hdf5 \
  --output_dir VLA/data/lerobot/square_d0_lerobot10d \
  --action-dim 10 --n_demo 200
```

### 验证 (服务器实跑结果)
```
357M	VLA/data/lerobot/square_d0_lerobot7d
357M	VLA/data/lerobot/square_d0_lerobot10d

square_d0_lerobot7d: 200 demos, 30479 frames
square_d0_lerobot10d: 200 demos, 30479 frames
```

### Round-trip 单测 (5/5 都过) ✅

**7D (action_dim=7):**
```
[T1] dataset loaded: 30479 frames, cols = ['observation.image', 'observation.wrist_image', 'observation.state', 'action']
[T2] image/wrist = (84, 84, 3)    (H, W, 3)
[T2] state        = (8,)  (8,)
[T2] action       = (7,) (7,)
[T3] image range over 20 samples: [6, 255] uint8 OK
[T3] state/action finite float32 OK
[T4] state_dim  = 8  (consistent)
[T4] action_dim = 7 (consistent)
[T5] 7 -> 10 -> 7 geodesic: worst=0.00e+00 rad (0.00e+00 deg), median=0.00e+00 rad
     over 26/50 well-conditioned samples (skipped: 24 |aa|~pi, 0 geo~pi)

[OK] All 5 tests passed (action_dim=7)
```

**10D (action_dim=10):**
```
[T1] dataset loaded: 30479 frames, cols = ['observation.image', 'observation.wrist_image', 'observation.state', 'action']
[T2] image/wrist = (84, 84, 3)    (H, W, 3)
[T2] state        = (8,)  (8,)
[T2] action       = (10,) (10,)
[T3] image range over 20 samples: [6, 255] uint8 OK
[T3] state/action finite float32 OK
[T4] state_dim  = 8  (consistent)
[T4] action_dim = 10 (consistent)
[T5] 10 -> 7 -> 10 geodesic: worst=0.00e+00 rad (0.00e+00 deg), median=0.00e+00 rad
     over 26/50 well-conditioned samples (skipped: 24 |aa|~pi, 0 geo~pi)

[OK] All 5 tests passed (action_dim=10)
```

**Round-trip 7→10→7 和 10→7→10 都 0 rad 误差 (完美 round-trip)** ✅

---

## 阶段 1: 4 路训练 (生产配置, 6-12h)

### 关键改动 (本机已加, 服务器 yaml 路径需修正)

#### 1.1 train 加 sticky `phase2_activated` 标志 ✅

**文件**: `VLA-v1.1/E_cti/train/padp_for_test_train.py` (VLA 副本 1:1 同步)

```python
# Line 548-550 (epoch 循环前)
phase2_activated = False  # sticky: 一旦 avg_loss < loss_threshold 永不再回退

# Line 604-609 (rollout 触发前)
if not phase2_activated and avg < loss_threshold:
    phase2_activated = True
    logger.info("[Phase2] epoch=%d 激活 Phase 2 (loss=%.4f < threshold=%.4f, sticky 永不再回退)",
                epoch, avg, loss_threshold)

# Line 614 (触发条件: avg < loss_threshold → phase2_activated)
and phase2_activated  # ⭐ sticky: 一旦激活永不再回退
```

#### 4 个 yaml 统一配置 ✅

| 文件 | loss_threshold | skip_rollout | enabled | interval | first_epoch | port |
|---|---|---|---|---|---|---|
| `padp_for_test_golden.yaml` | 0.01 | false | true | 5 | 5 | 8765 |
| `padp_for_test_7d_golden.yaml` | 0.01 | false | true | 5 | 5 | 8765 |
| `lerobot_libero7d.yaml` | 0.01 | false | true | 5 | 5 | 8765 |
| `lerobot_libero10d.yaml` | 0.01 | false | true | 5 | 5 | 8765 |

#### 服务器路径修正 (本机绝对路径 → 远程绝对路径) ⏳

本机之前在 yaml 写的是 `/home/hy/Desktop/PADP_v3/VLA/...`, 服务器在 `/media/disk7t/PADP_v3/`, **需在服务器上跑 sed 修正**:

```bash
# CWD = /media/disk7t/PADP_v3
# 备份
cp VLA/E_cti/configs/padp_for_test_golden.yaml VLA/E_cti/configs/padp_for_test_golden.yaml.bak
cp VLA/E_cti/configs/padp_for_test_7d_golden.yaml VLA/E_cti/configs/padp_for_test_7d_golden.yaml.bak
cp VLA/E_cti/configs/lerobot_libero7d.yaml VLA/E_cti/configs/lerobot_libero7d.yaml.bak
cp VLA/E_cti/configs/lerobot_libero10d.yaml VLA/E_cti/configs/lerobot_libero10d.yaml.bak

# 改 dataset_path: /home/hy/Desktop/PADP_v3/VLA → /media/disk7t/PADP_v3/VLA
sed -i 's|/home/hy/Desktop/PADP_v3/VLA|/media/disk7t/PADP_v3/VLA|g' \
  VLA/E_cti/configs/padp_for_test_golden.yaml \
  VLA/E_cti/configs/padp_for_test_7d_golden.yaml

# 改 ckpt_dir (相对 → 绝对, 因为 CWD=VLA/ 解析不到 data/outputs/)
sed -i 's|^  ckpt_dir: data/outputs/|  ckpt_dir: /media/disk7t/PADP_v3/data/outputs/|' \
  VLA/E_cti/configs/padp_for_test_golden.yaml \
  VLA/E_cti/configs/padp_for_test_7d_golden.yaml \
  VLA/E_cti/configs/lerobot_libero7d.yaml \
  VLA/E_cti/configs/lerobot_libero10d.yaml

# 验证
grep -E "dataset_path|ckpt_dir" VLA/E_cti/configs/*.yaml | grep -v bak
```

### 4 个终端训练命令 (CWD = /media/disk7t/PADP_v3/VLA, 命令相对 E_cti/train 位置)

**Terminal 1 (GPU 0) — A. 1.1_10D:**
```bash
# CWD = /media/disk7t/PADP_v3/VLA
CUDA_VISIBLE_DEVICES=0 /opt/miniconda3/envs/equidiff/bin/python -u \
  E_cti/train/padp_for_test_train.py \
  --config E_cti/configs/padp_for_test_golden.yaml \
  --max_epochs 251 --rollout_port 8765
```

**Terminal 2 (GPU 0) — B. 1.1_7D:**
```bash
# CWD = /media/disk7t/PADP_v3/VLA
CUDA_VISIBLE_DEVICES=0 /opt/miniconda3/envs/equidiff/bin/python -u \
  E_cti/train/padp_for_test_train.py \
  --config E_cti/configs/padp_for_test_7d_golden.yaml \
  --max_epochs 251 --rollout_port 8764
```

**Terminal 3 (GPU 1) — C. 1.2_10D:**
```bash
# CWD = /media/disk7t/PADP_v3/VLA
CUDA_VISIBLE_DEVICES=1 /opt/miniconda3/envs/equidiff/bin/python -u \
  E_cti/train/padp_for_libero_train.py \
  --config E_cti/configs/lerobot_libero10d.yaml \
  --max_epochs 251
```

**Terminal 4 (GPU 1) — D. 1.2_7D (默认):**
```bash
# CWD = /media/disk7t/PADP_v3/VLA
CUDA_VISIBLE_DEVICES=1 /opt/miniconda3/envs/equidiff/bin/python -u \
  E_cti/train/padp_for_libero_train.py \
  --max_epochs 251
```

### 预期 log 行为 (sticky phase2)

- **前 N epoch** `avg >= 0.01`: log 出现 `[Rollout] epoch=X 跳过 (Phase 1: ...)` 
- **某 epoch** `avg < 0.01`: log 出现 `[Phase2] epoch=X 激活 Phase 2 (loss=... < threshold=0.01, sticky 永不再回退)`, **只出现一次**
- **此后** 每 5 epoch 跑 rollout, log 出现 `[Rollout] epoch=X 跑 ...`
- 即使后续 `avg` 反弹到 0.02, **不再出现** "跳过" / "激活" log (sticky 永不再触发)

---

## 阶段 2: 4 路 sim rollout (训练完, 5-10 min/路)

服务器上 4 终端同时跑 (CWD = `/media/disk7t/PADP_v3`):
```bash
# A. 1.1_10D
/opt/miniconda3/envs/equidiff/bin/python Agent信息存储/verify_lerobot_rollout.py \
  --line 1.1 --action_dim 10 --port 8765 --n_train 4 --n_test 8 --max_steps 400

# B. 1.1_7D
... --line 1.1 --action_dim 7 --port 8764 ...
# C. 1.2_10D
... --line 1.2 --action_dim 10 --port 8763 ...
# D. 1.2_7D
... --line 1.2 --action_dim 7 --port 8762 ...
```

---

## 进度状态

- ✅ **阶段 0**: 4 路数据生成 + round-trip 单测 (200 demos × 2 datasets × 5/5 测试都过)
- ⏳ **阶段 1**: 4 路训练 (待服务器 sed 修正 yaml 路径 + 启动 4 终端训练)
- ⏳ **阶段 2**: 4 路 sim rollout

---

## 关键文件清单 (本机 `/home/hy/Desktop/PADP_v3/`)

### 新建 (v1.2 lerobot 线, 1:1 跟 1.1 互不污染)
- `VLA/C_sim/robomimic/padp_for_libero_dataset.py` — lerobot parquet loader
- `VLA/B_model/encoders/VM/lerobot_obs_encoder.py` — rgb_key remap
- `VLA/E_cti/train/padp_for_libero_train.py` — train wrapper (monkey-patch)
- `VLA/E_cti/train/padp_for_libero_server.py` — server wrapper (monkey-patch + port auto-avoid)
- `VLA/F_envs/robomimic/lerobot_robomimic_env.py` — v1.3 包装类 + 5 项单测
- `VLA/E_cti/train/port_utils.py` — server 端口 auto-avoid helper
- `Agent信息存储/verify_lerobot_rollout.py` — 4 路消融验证 wrapper

### 改动 (1.1 线, 加 sticky phase2 + 4 yaml 统一)
- `VLA-v1.1/E_cti/train/padp_for_test_train.py` — sticky phase2_activated (VLA 副本 1:1 同步)
- `VLA/F_envs/robomimic/tcp_rollout_client.py` — `--action_dim` (7D/10D 都支持)
- `VLA/C_sim/robomimic/padp_for_test_dataset.py` — config-driven 维度校验
- `VLA/C_sim/robomimic/padp_for_libero_dataset.py` — config 维度校验 raise
- 4 个 yaml — `loss_threshold=0.01` + `interval=5` + `enabled=true` 全统一

### 红线 (不动)
- `VLA-v1.1/E_cti/train/padp_for_test_server.py` — 1.1 server 干净版
- `VLA-v1.1/E_cti/train/padp_for_test_rollout.py` — 1.1 rollout 干净版
- `VLA/E_cti/train/padp_for_test_server.py` — 1.1 server (跟 VLA-v1.1 1:1)

### 配置文件
- `VLA/E_cti/configs/padp_for_test_golden.yaml` (A. 1.1_10D)
- `VLA/E_cti/configs/padp_for_test_7d_golden.yaml` (B. 1.1_7D)
- `VLA/E_cti/configs/lerobot_libero10d.yaml` (C. 1.2_10D)
- `VLA/E_cti/configs/lerobot_libero7d.yaml` (D. 1.2_7D, 默认)

---

## 服务器路径速查表 (相对路径版本)

服务器实际挂载点: `/media/disk7t/PADP_v3/`

| 用途 | CWD | 相对路径 |
|---|---|---|
| 阶段 0 (数据生成) | `/media/disk7t/PADP_v3` | `VLA/C_sim/robomimic/convert_to_lerobot.py` |
| 阶段 0 (round-trip) | `/media/disk7t/PADP_v3` | `VLA/C_sim/robomimic/test_lerobot_round_trip.py` |
| 阶段 1 (训练) | `/media/disk7t/PADP_v3/VLA` | `E_cti/train/padp_for_test_train.py` / `E_cti/train/padp_for_libero_train.py` |
| 阶段 2 (rollout) | `/media/disk7t/PADP_v3` | `Agent信息存储/verify_lerobot_rollout.py` |
| 4 个 yaml 配置 | `/media/disk7t/PADP_v3/VLA` | `E_cti/configs/*.yaml` |
| ckpt 输出 | (相对 PADP_v3 根) | `data/outputs/{line}/` |
| hdf5 源 | (相对 VLA 根) | `VLA/data/robomimic/datasets/square_d0/square_d0_abs.hdf5` |
| lerobot 数据 | (相对 VLA 根) | `VLA/data/lerobot/square_d0_lerobot{7d,10d}/` |

**所有命令都用相对路径**, 不会因 mount 路径变化 (例如 `/home/hy/Desktop/PADP_v3` vs `/media/disk7t/PADP_v3`) 失效。
