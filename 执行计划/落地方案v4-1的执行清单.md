# 落地方案v4-1 的执行清单(逐文件进度跟踪)

> 配套文档:
> - 上位架构:[落地方案v4.md](落地方案v4.md)
> - 阶段 1 实施手册(最终版):[落地方案v4-1.md](落地方案v4-1.md)
> - 文件级 Todo(已合 4 个铁律):[落地方案v4-1的Todo清单.md](落地方案v4-1的Todo清单.md)
> - 新仓库:`c:\QClaw\参与修改\VLA\`
>
> **本文件定位**:逐文件**实施进度**跟踪(开发中,谁写完就打勾)。

---

## 0. 总进度

| 层级 | 文件数(v4 重构后) | 已完成 |
|---|---|---|
| 根目录 | 5 | 5/5 ✅ |
| F_envs | 4 | 4/4 ✅ |
| A_common | 19(+1:`base_policy.py`) | 19/19 ✅ |
| B_model | 8(-4:`compose/` 整体迁出) | 8/8 ✅ |
| C_sim | 11 | 11/11 ✅ |
| D_real | 1 | 1/1 ✅ |
| E_cti | 6 | 6/6 ✅ |
| G_algo | 8(+2:`padp_policy.py` / `dp_policy.py`,+1:`fusion.py`) | 8/8 ✅ |
| **总计** | **~62** | **~62/62** ✅ |

> **v4 重构后**:`B_model/compose/` 整体迁出,文件数 −4;`A_common/types/base_policy.py` + `G_algo/PADP/padp_policy.py` + `G_algo/DP/dp_policy.py` + `G_algo/common/fusion.py` 在新位置就绪。所有 7-Layer 契约代码就绪。**接下来**:数据准备 + 训练 + 评估。

---

## 1. 根目录(5 个)

- [x] `VLA/__init__.py` — 包根
- [x] `VLA/README.md` — 快速开始
- [x] `VLA/.gitignore`(待补)
- [x] `VLA/E_cti/configs/train_padp.yaml` — PADP 训练超参
- [x] `VLA/E_cti/configs/train_dp.yaml` — DP baseline 训练超参

## 2. F_envs/(4 个)

- [x] `VLA/F_envs/__init__.py`
- [x] `VLA/F_envs/base_train/__init__.py`
- [x] `VLA/F_envs/base_train/environment.yml` — **纯 conda 单一文件(铁律 7)**
- [x] `VLA/F_envs/base_train/README.md`

## 3. A_common/(19 个,v4 重构后 +1)

- [x] `A_common/__init__.py`
- [x] `A_common/types/__init__.py`
- [x] `A_common/types/action_output.py` — **契约 1**
- [x] `A_common/types/observation.py`
- [x] `A_common/types/state.py`
- [x] `A_common/types/normalizer.py` — 单字段 LinearNormalizer(阶段 1 简化)
- [x] `A_common/types/normalizer_utils.py` — range/image/abs normalizers
- [x] **`A_common/types/base_policy.py`** — **(v4 重构后从 B_model/compose/ 上移)** 契约 2
- [x] `A_common/registry/__init__.py`
- [x] `A_common/registry/policy_registry.py` — **契约 3**
- [x] `A_common/data/__init__.py`
- [x] `A_common/data/base_dataset.py` — **铁律 6:BaseVLADataset + SequenceSampler(edge_repeat padding)**
- [x] `A_common/data/base_collator.py`
- [x] `A_common/ipc/__init__.py` — 阶段 1 占位
- [x] `A_common/logger/__init__.py` — Python 标准 logging
- [x] `A_common/ckpt/__init__.py` — save/load torch checkpoint
- [x] `A_common/ckpt/ema.py` — bug fix 4:抄 PADP `ema_model.py`
- [x] `A_common/tests/__init__.py`
- [x] `A_common/tests/test_action_output.py` — 契约 1 测试
- [x] `A_common/tests/test_policy_registry.py` — 契约 3 测试

## 4. B_model/(8 个,v4 重构后 -4)

- [x] `B_model/__init__.py`
- [x] `B_model/encoders/__init__.py`
- [x] `B_model/encoders/VM/__init__.py`
- [x] `B_model/encoders/VM/robomimic_obs_encoder.py` — **VM 实现**(沿用 PADP)
- [x] `B_model/encoders/AM/__init__.py` — 阶段 1 占位
- [x] `B_model/encoders/TM/__init__.py` — 阶段 1 占位
- [x] `B_model/networks/__init__.py`
- [x] `B_model/networks/DM/__init__.py`
- [x] `B_model/networks/DM/unet1d_padp.py` — **核心:1D UNet PADP 版** (conv1d_components inline 进来)
- [x] `B_model/adapters/__init__.py` — 阶段 1 占位

> **v4 重构后 B_model 终止于上一行**。`B_model/compose/` 整体迁出(4 文件 → 4 新位置),见下方 A_common / G_algo 章节。
> - ~~`B_model/compose/__init__.py`~~ → 删除
> - ~~`B_model/compose/base_policy.py`~~ → 迁至 `A_common/types/base_policy.py`(契约 2)
> - ~~`B_model/compose/padp_policy.py`~~ → 迁至 `G_algo/PADP/padp_policy.py`
> - ~~`B_model/compose/dp_policy.py`~~ → 迁至 `G_algo/DP/dp_policy.py`

## 5. C_sim/(11 个)

- [x] `C_sim/__init__.py` — **铁律 4 factory 入口**
- [x] `C_sim/robomimic/__init__.py` — 内部
- [x] `C_sim/robomimic/env_impl/__init__.py`
- [x] `C_sim/robomimic/env_impl/env_meta.py` — 23 task 元信息(抄 PADP)
- [x] `C_sim/robomimic/env_impl/wrappers.py` — gym wrapper + make_env
- [x] `C_sim/robomimic/zarr_dataset.py` — **铁律 6:RobomimicZarrDataset + SequenceSampler**
- [x] `C_sim/robomimic/env_runner.py` — RobomimicImageRunner
- [x] `C_sim/pusht/__init__.py` — 阶段 1 占位
- [x] `C_sim/kitchen/__init__.py` — 阶段 1 占位
- [x] `C_sim/shared/__init__.py` — 阶段 1 占位

## 6. D_real/(1 个)

- [x] `D_real/__init__.py` — 阶段 1 占位

## 7. E_cti/(6 个)

- [x] `E_cti/__init__.py`
- [x] `E_cti/configs/__init__.py`
- [x] `E_cti/configs/train_padp.yaml` — **PADP 训练超参**
- [x] `E_cti/configs/train_dp.yaml` — DP baseline
- [x] `E_cti/train/__init__.py`
- [x] `E_cti/train/run_train.py` — **纯脚本,铁律 1 满足**
- [x] `E_cti/train/run_eval.py` — **铁律 4 满足(只 `import C_sim`)**
- [x] `E_cti/collect/__init__.py` — 阶段 1 占位
- [x] `E_cti/deploy/__init__.py` — 阶段 1 占位

## 8. G_algo/(8 个,v4 重构后 +2)

- [x] `G_algo/__init__.py`
- [x] `G_algo/common/__init__.py`
- [x] `G_algo/common/position_noise.py` — **铁律 5:add_position_noise 共享**
- [x] `G_algo/common/fusion.py` — **(v4 重构后从 B_model/compose/fusion.py 迁来)** FiLM / cross-attn 工具
- [x] `G_algo/DP/__init__.py`
- [x] `G_algo/DP/dp_pipeline.py` — **baseline DP compute_loss**
- [x] `G_algo/DP/dp_policy.py` — **(v4 重构后从 B_model/compose/dp_policy.py 迁来)** `@register_policy("dp_unet")` 继承 padp
- [x] `G_algo/PADP/__init__.py`
- [x] `G_algo/PADP/padp_pipeline.py` — **PADP compute_loss(per-position weighted)**
- [x] `G_algo/PADP/padp_policy.py` — **(v4 重构后从 B_model/compose/padp_policy.py 整体迁来)** `@register_policy("padp_unet")` + sliding window 推理
- [x] `G_algo/PADP/loss_weights.py` — per-position 加权曲线
- [x] `G_algo/PADP/metrics.py` — per-position MSE / NMSE

---

## 9. 实施进度时间线

| 时间 | 事件 |
|---|---|
| T0 | 读 PADP 关键源文件完成(model / dataset / env / config) |
| T1 | 7-Layer 契约代码就绪(62 文件,v4 重构后,本清单全部打勾) |
| T2 | 数据准备:`square_d0_abs.hdf5` 放到 `data/robomimic/datasets/square_d0/` |
| T3 | `conda env create -f F_envs/base_train/environment.yml` |
| T4 | `pytest A_common/tests/ -v` 通过 |
| T5 | `python E_cti/train/run_train.py --config E_cti/configs/train_padp.yaml` 训练到 ≥ 90% |
| T6 | `python E_cti/train/run_eval.py --config E_cti/configs/train_padp.yaml --ckpt data/outputs/padp_vla/latest.ckpt --n_test 50` 验证 ≥ 0.90 |
| T7 | DP baseline 跑通,差距 ≤ 3% |

---

## 10. 7-Layer 验收检查(commit 前跑)

```bash
# 铁律 1: E_cti 无 class
[ -z "$(grep -rE '^class ' E_cti/)" ] && echo "✓ 1" || echo "✗ 1"

# 铁律 2: v4 重构后版本
#   (a) B_model/compose/ 目录不应存在
[ ! -d B_model/compose ] && echo "✓ 2a" || echo "✗ 2a"
#   (b) B_model 不 import G_algo / E_cti
[ -z "$(grep -rE 'import G_algo|import E_cti' B_model/)" ] && echo "✓ 2b" || echo "✗ 2b"
#   (c) B_model 不含 Policy 类 / 算法专属 buffer
[ -z "$(grep -rE '@register_policy|class .*Policy\(|sliding.window|alpha_bar|inference_buffer' B_model/)" ] && echo "✓ 2c" || echo "✗ 2c"

# 铁律 3: 数据结构 + protocol/interface 在 A_common/types/(含 BaseVLAPolicy)
[ -z "$(grep -rE 'class ActionOutput|class Observation|class State|class BaseVLAPolicy' B_model/ C_sim/ E_cti/ G_algo/)" ] && echo "✓ 3" || echo "✗ 3"
[ -f A_common/types/base_policy.py ] && echo "✓ 3b" || echo "✗ 3b (缺 base_policy.py)"

# 铁律 4: E_cti 只 import C_sim 顶层
[ -z "$(grep -rE 'from C_sim\.[a-z_]+(\.[a-z_]+)+ import' E_cti/)" ] && echo "✓ 4" || echo "✗ 4"

# 铁律 5: G_algo/common/denoise_loop.py 不硬编码 DDIMScheduler(scheduler 注入)
[ -z "$(grep -E 'DDIMScheduler\(|import.*DDIMScheduler|from diffusers' G_algo/common/denoise_loop.py)" ] && echo "✓ 5" || echo "✗ 5"

# 铁律 6: base_dataset.py 显式声明 n_obs_steps / horizon
grep -E "n_obs_steps|horizon" A_common/data/base_dataset.py && echo "✓ 6" || echo "✗ 6"

# 铁律 7: F_envs 纯 conda(无 requirements.txt)
[ ! -f F_envs/base_train/requirements.txt ] && echo "✓ 7" || echo "✗ 7"
grep -E "pytorch::pytorch" F_envs/base_train/environment.yml && echo "✓ 7b (conda 频道)" || echo "✗ 7b"
```

预期:**全部 `OK`(8 行,含 2a/2b/2c/3/3b/4/5/6/7/7b)**。

---

## 11. 实际运行命令(下一步)

见 [落地方案v4-1.md §三](落地方案v4-1.md) 与 `VLA/README.md`。

---

## 12. 实际修复记录(对照 PADP v3 工作版 + 7 铁律 + v4 重构)

| # | 修复 | 涉及文件 | 验证 |
|---|---|---|---|
| **0** | **v4 重构:合掉 B_model/compose** | 4 文件迁移:base_policy → A_common/types/,padp_policy → G_algo/PADP/,dp_policy → G_algo/DP/,fusion → G_algo/common/;`B_model/compose/` 删除 | 7-Layer §10 验收脚本 2a/b/c 全 OK;`B_model/compose/` 不存在;`A_common/registry/policy_registry.py` 不再 import B_model |
| 1 | 漏传 3 个 obs_encoder kwarg | `G_algo/PADP/padp_policy.py`(v4 重构后位置) | `padp_policy.__init__` 接受 `crop_shape` / `obs_encoder_group_norm` / `eval_fixed_crop` |
| 2 | `n_action_steps: 8 → 1` | `E_cti/configs/train_padp.yaml` + `train_dp.yaml` | 对齐 PADP v3 单步滑窗默认 |
| 3 | `window_exp_gamma: 0.2 → 0.25` | `E_cti/configs/train_padp.yaml` | 对齐用户命令 |
| 4 | 新增 EMA 实现 | `A_common/ckpt/ema.py`(抄 PADP `ema_model.py` 88 行版)+ 接入 `E_cti/train/run_train.py` + `run_eval.py` | 训练时每步 `ema.step(policy)`;eval 时自动加载 EMA 权重 |
| 5 | 35 个 `__init__.py` 加 `# -*- coding: utf-8 -*-` 声明 | 35 个 __init__.py | 避 Python 2.7 / Linux GBK 编码报错 |
| 6 | `test_policy_registry.py` 修私有变量 import | `A_common/tests/test_policy_registry.py` | `from A_common.registry.policy_registry import _POLICY_REGISTRY` |
| 7 | 新增 `scripts/sync_todo.py` | `VLA/scripts/sync_todo.py` | Todo ↔ 文件系统 diff,支持 `--json` 和 `--apply-stub` |

---

## 13. 文档状态(本目录 10 个文件)

| 文件 | 状态 | 说明 |
|---|---|---|
| `落地方案.md` (v1) | **已归档** | 早期版,内容已合到 v4-1 |
| `落地方案v2.md` | **已归档** | 早期版 |
| `落地方案v3.md` | **已归档** | 三阶段首版 |
| `落地方案v4.md` | **现行规范** | 7-Layer 架构 + 7 铁律(v4 重构版:B_model 去 compose) |
| `落地方案v4-1.md` | **当前阶段 1 实施手册** | 59 文件(v4 重构后),7 铁律,4 bug 修复,EMA |
| `落地方案v4-1.1.md` | **已归档** | 59 文件精简版,v4 重构后变更已合到 v4-1 |
| `落地方案v4-1的Todo清单.md` | **文件级 Todo** | 与 VLA/ 59 文件结构同步 |
| `落地方案v4-1的执行清单.md` | **本文件** | 进度跟踪 + 修复记录 + 7-Layer 验收 |
| `与现有代码的映射关系.md` | **PADP/GuidedVLA → VLA 映射** | 文件级对应表(v4 重构后) |
| `用户执行方案.md` | **操作手册** | 命令 + AI 提示词模板 + 优化路径 |

